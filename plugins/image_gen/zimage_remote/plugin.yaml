name: zimage_remote
version: 1.0.0
description: "Private remote GPU Z-Image worker backend. Hermes remains the control plane; the worker handles txt2img generation and returns stable metadata."
author: Hermes
kind: backend
requires_env:
  - ZIMAGE_REMOTE_BASE_URL
