# Hermes v2026.5.16 Upstream-First Upgrade

> **TL;DR** — This upgrade starts from upstream `v2026.5.16` and ports local
> behavior only after it proves it is still needed. Local patches are not
> preserved by default; upstream-equivalent or plugin/config-representable
> changes are discarded from core.

## Baseline

- Worktree: `/Users/simon/.hermes/hermes-agent/.worktrees/upgrade-v2026.5.16-upstream-base`
- Branch: `upgrade/v2026.5.16-upstream-base`
- Base: `v2026.5.16` / `a91a57fa5`
- Previous local branch: `feat/hermes-custom-autonomy` / `18e349d11`
- Previous upstream baseline: `v2026.5.7`

Inventory inputs:

- Local delta: `git diff-tree --stat --no-commit-id -r v2026.5.7 feat/hermes-custom-autonomy`
- Upstream replacement window: `git log --oneline v2026.5.7..v2026.5.16`
- Upgrade rule: use upstream as base; port only tested, still-required local behavior.

## Local Delta Decisions

| Surface | Current local source | Initial decision | Reason |
| --- | --- | --- | --- |
| Context compression aux fallback | `agent/context_compressor.py`, `tests/agent/test_context_compressor.py`, commit `ee0dbf791` | `upstream-equivalent` | Upstream `v2026.5.16` already has main-model fallback paths, malformed/streaming error handling, and tests for broken aux models. Do not port unless live compression smoke finds a gap. |
| Cron jobs-file update race | `cron/jobs.py`, `tests/cron/test_jobs.py`, commit `1cf03a95a` | `port-core-required` | TDD audit found upstream still allowed concurrent `update_job` load-modify-save overlap. Ported the minimal `RLock` + CRUD cycle locking fix; `tests/cron/test_jobs.py` passes. |
| Cron artifact delivery audit gate | `cron/scheduler.py`, `tests/cron/test_scheduler.py`, commit `18e349d11` | `port-core-required` | Upstream has no equivalent post-output/pre-delivery artifact audit hook. Ported the fail-closed `delivery_audit` gate into upstream's current `tick()` flow without carrying the older `execute_job` restructuring. |
| Cron configured provider routing | `cron/scheduler.py`, commit `95feadb45` | `upstream-equivalent` | Upstream now avoids injecting stale `HERMES_INFERENCE_PROVIDER` into cron runtime resolution and lets `resolve_runtime_provider()` honor persisted config precedence. Verified `tests/cron/test_codex_execution_paths.py`; no local port needed. |
| Closed stdout max-iteration summary | `run_agent.py`, `tests/run_agent/test_run_agent.py`, commit `68f3aca82` | `port-core-required` | Upstream still used direct `print()` in `_handle_max_iterations`. Ported the focused `_safe_print()` call after a RED test that patches closed stdout. |
| Post-stream placeholder media fallback | `gateway/run.py`, `tests/gateway/test_post_stream_media_delivery.py`, commit `7bffa291b` | `port-core-required` | Upstream still routed unresolved `MEDIA:<placeholder>` to file senders. Ported a placeholder-only fallback so existing TTS route-selection tests remain unchanged. |
| Layer-2 memory and review tools | `memory/layer2_*`, `agent/layer2_*`, `tools/layer2_*`, tests under `tests/memory` and `tests/tools` | `port-as-plugin/provider` | Keep the product idea only if Taiwan cron still needs it. Do not re-add core files by default; use memory-provider/tool plugin boundaries. |
| Local `/loop` runtime | `hermes_loop/*`, `hermes_cli/loop.py`, gateway recovery tests | `discard-core-by-default` | Upstream has `/goal`, `/subgoal`, Kanban, checkpoints, and gateway recovery primitives. Rebuild only missing evidence-gate behavior after a failing test proves upstream cannot cover it. |
| Gateway recovery additions | `gateway/run.py`, `tests/gateway/test_loop_recovery.py` | `audit-before-port` | Much of this is tied to local loop. Keep only generic restart/session recovery fixes not covered by upstream circuit breaker, `/platform`, checkpoints, or Kanban. |
| Slack thread delivery and edit guards | `gateway/platforms/slack.py`, `tools/send_message_tool.py`, Slack tests | `port-core-required` | TDD audit found upstream still missed `send_message` Slack `channel:thread_ts` targets and local oversized edit rejection before Slack API calls. Ported only those focused behaviors. |
| Image reference inputs | `agent/image_routing.py`, `tools/image_generation_tool.py`, image-gen provider tests, `gateway/run.py` | `port-core-required-minimal` | TDD audit found upstream has native vision routing but no `image_generate` reference-image contract. Ported only current-turn uploaded image references, plugin dispatch, and OpenAI/OpenAI-Codex provider support. Arbitrary local paths and `local_ref:*` stay out of core unless a separate config/plugin slice proves they are needed. |
| Story/video and Taiwan creative skills | `skills/creative/*` | `move-to-runtime-skills` | These are local workflow assets, not upstream-base core. Keep under runtime skill directories or a local plugin, not as release-upgrade core patches. |
| Release/adoption docs | `docs/plans/*`, design docs | `discard-from-core` | Historical local planning docs are useful context but should not be ported into upstream-base unless they document active repo behavior. |
| Dashboard plugin discovery gate | `hermes_cli/web_server.py`, `tests/hermes_cli/test_web_server.py`, commit `18f1b61b5` | `port-core-required` | Upstream has dashboard plugin auth/visibility hardening, but user/project dashboard plugin discovery still ignored `plugins.enabled`/`plugins.disabled`. Ported the config gate without changing bundled plugin defaults. |
| Security hardening patches | dashboard auth, SSRF, quick-command redaction | `upstream-equivalent` | Release notes and upstream files show these landed upstream. Do not port local copies. |
| Image token estimation for native image messages | `agent/model_metadata.py`, `gateway/run.py`, commit `8fb56929b` | `upstream-equivalent` | Upstream already has bounded image-token accounting and base64-stripped token estimation. Verified `tests/agent/test_model_metadata.py`; no local port needed. |
| Cron/Taiwan operating model | `docs/plans/2026-05-11-tw-stock-cron-operating-model.md`, cron runtime state | `runtime-config-not-core` | Keep the operating model in runtime instructions/state. Only port code if a current cron job fails under upstream behavior. |
| AGENTS upgrade/TDD rules | `AGENTS.md` | `port-doc` | This is local operator policy. Keep it as the first intentional doc patch on the upgrade branch. |

## Final Convergence Closure

The upgrade is closed from a core-port perspective. The branch keeps only the
local changes that still lack an upstream-equivalent mechanism and have focused
tests. Everything else from `feat/hermes-custom-autonomy` stays out of core
unless a future failing test proves a current live-runtime contract.

| Old local surface | Closure | Replacement path |
| --- | --- | --- |
| Layer-2 memory and review tools | `pluginize-later-not-release-blocker` | Upstream has `agent.memory_provider`, `hermes memory setup/status`, provider tool routing, bundled memory plugins, and cron `wakeAgent` gating. Rebuild as an external memory provider/plugin only if Taiwan cron still needs it. |
| Local `/loop` runtime | `do-not-port-core` | Use upstream `/goal`, `/subgoal`, gateway Kanban dispatch, checkpoints, and `resume_pending` session recovery. Add a new upstream-base failing test before rebuilding any missing evidence-gate behavior. |
| Loop-specific gateway recovery | `do-not-port-core` | Keep only generic recovery bugs already ported, such as placeholder-media fallback and closed-stdout handling. Do not revive loop-specific recovery state. |
| Story/video and Taiwan creative skills | `move-to-runtime-skills` | Keep these as runtime skills or a local plugin under Hermes home, not release-upgrade core patches. |
| Arbitrary local image references | `discard-from-core` | Support only current-turn uploaded references in `image_generate`. Local filesystem references stay rejected unless a separate opt-in plugin/config design proves necessary. |
| Release/adoption planning docs | `discard-from-core` | Keep historical plans as context only. Do not copy old planning docs into the upstream-base branch unless they describe active behavior. |
| Old security hardening patches | `upstream-equivalent` | Dashboard plugin auth, quick-command redaction, SSRF coverage, and image token estimation are handled upstream. Keep local copies out of this branch. |
| Taiwan cron operating model | `runtime-config-not-core` | Keep product policy in runtime instructions/state. Port code only after a current cron test fails under upstream behavior. |

## Execution Status

1. Started from upstream tag `v2026.5.16`.
2. Ported `AGENTS.md` upgrade/TDD policy as a docs-only local operator patch.
3. Audited old local commits from `v2026.5.7..feat/hermes-custom-autonomy`.
4. Ported only focused core fixes with Red-Green-Refactor coverage:
   cron jobs-file locking, Slack thread/edit guards, cron delivery audit,
   dashboard plugin enablement, closed stdout, post-stream media fallback, and
   current-turn image references.
5. Rejected the remaining local surfaces from core because upstream already has
   equivalent mechanisms or a plugin/config/runtime-state path.
6. Restarted the live gateway from the upstream-base branch after tests passed.
7. Pushed the integration branch to `origin`.

## Final Verification

The closure check targets the upstream mechanisms that replace the discarded
local core patches:

```bash
venv/bin/python -m pytest \
  tests/agent/test_memory_provider.py \
  tests/cron/test_scheduler.py::TestParseWakeGate \
  tests/cron/test_scheduler.py::TestRunJobWakeGate \
  tests/gateway/test_goal_verdict_send.py \
  tests/gateway/test_restart_resume_pending.py \
  tests/hermes_cli/test_kanban_core_functionality.py::test_config_default_dispatch_in_gateway_is_true \
  tests/hermes_cli/test_kanban_core_functionality.py::test_reclaim_task_resets_running_to_ready \
  -q
```

Result: `152 passed in 12.99s`.

## TDD Gate

This inventory is documentation-only, so TDD does not apply to this file.
Every code port from the local branch must follow Red-Green-Refactor:

1. Write or identify a focused upstream-base test that fails for the missing
   local behavior.
2. Confirm the failure reason.
3. Port the minimal implementation.
4. Re-run the targeted test and the nearest suite.
5. Refactor only after green.
